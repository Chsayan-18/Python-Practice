# Write a program to input user's first name & print it's length.

name = input("Enter your first name : ")
print(len(name))

# Write a program to find the occurrence of "$" in a String
str = "Hello $ there, how are $you doing today? $$"
print(str.count("$"))